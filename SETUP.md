# 홈트 · 아파트 무소음 루틴 앱

주 5회 20분 홈트레이닝을 위한 정적 웹앱입니다. 외부 라이브러리를 하나도 쓰지 않아서
GitHub Pages에 파일만 올리면 바로 동작합니다.

## 화면 5개

| 탭 | 파일 | 내용 |
|---|---|---|
| 오늘 | `pages/tracker.html` | 12주 체크표 · 날짜별 운동 체크 · 연속일 계산 · 인터벌 타이머 |
| 루틴 | `pages/routine.html` | A/B/C 세션 구성과 근거, 4주 진행 계획 |
| 동작 | `pages/moves.html` | 본 운동 15가지 그림 설명 |
| 스트레칭 | `pages/stretch.html` | 워밍업·마무리·추가 운동 25가지 그림 설명 |
| 식단 | `pages/diet.html` | 단백질 목표 계산기, 렌틸콩 가이드 |

## 올리는 방법

1. GitHub에서 새 저장소를 만듭니다. 이름은 아무거나 (예: `hometraining`).
   GitHub Pages 무료 사용은 **공개(Public) 저장소**여야 합니다.
   (앱에 개인 정보는 들어 있지 않습니다. 운동 기록은 저장소가 아니라 휴대폰 브라우저에만 저장돼요.)

2. 이 폴더 안의 **모든 파일과 폴더**를 저장소에 올립니다.
   - 웹에서: 저장소 화면 → `Add file` → `Upload files` → 폴더째 끌어다 놓기
   - 명령어로:
     ```
     git init
     git add .
     git commit -m "홈트 앱"
     git branch -M main
     git remote add origin https://github.com/<아이디>/<저장소이름>.git
     git push -u origin main
     ```

3. 저장소 → `Settings` → 왼쪽 메뉴 `Pages` →
   **Source: Deploy from a branch**, **Branch: main / (root)** → `Save`

4. 1~2분 뒤 주소가 나옵니다: `https://<아이디>.github.io/<저장소이름>/`

## 휴대폰 홈 화면에 추가

- **아이폰(Safari)** — 주소 열기 → 아래 공유 버튼 → `홈 화면에 추가`
- **안드로이드(Chrome)** — 주소 열기 → 오른쪽 위 ⋮ → `앱 설치` 또는 `홈 화면에 추가`

추가하고 나면 주소창 없는 전체화면으로 열립니다.
한 번 열어두면 인터넷이 끊겨도 동작해요 (서비스 워커가 파일을 캐시합니다).

## 운동 기록에 대해

기록은 **그 브라우저에만** 저장됩니다. GitHub에는 올라가지 않아요.

- 휴대폰과 PC의 기록은 서로 다릅니다.
- 브라우저 데이터를 지우면 기록도 지워집니다.
- 체크표 맨 아래 **기록 백업**에서 가끔 코드를 복사해 메모장에 붙여두세요.

## 내용을 고치고 싶을 때

`pages/` 안의 HTML 파일을 열어서 고치면 됩니다. 각 파일 안에 스타일과 스크립트가
모두 들어 있어서 다른 파일에 영향을 주지 않아요.

고친 뒤 GitHub에 다시 올리면 반영됩니다. 휴대폰에서 옛날 화면이 보이면
앱을 완전히 닫았다가 다시 열어보세요 (서비스 워커가 새 버전을 받아옵니다).

파일 목록을 바꿨다면 `sw.js` 위쪽의 `VERSION` 값을 `homefit-v2` 처럼 올려주세요.

## 파일 구조

```
├── index.html              앱 껍데기 (하단 탭 + 화면 전환)
├── manifest.webmanifest    앱 이름·아이콘·전체화면 설정
├── sw.js                   서비스 워커 (설치 및 오프라인)
├── .nojekyll               GitHub Pages가 파일을 그대로 서빙하게
├── icons/                  홈 화면 아이콘
└── pages/                  화면 5개
```
